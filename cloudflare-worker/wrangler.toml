import { defineWorkersConfig } from "@cloudflare/workers-types";
import type { Config } from "wrangler";

export default defineWorkersConfig({
  name: "novel-translator-worker",
  main: "src/worker.ts",
  compatibility_date: "2024-09-23",
  compatibility_flags: ["nodejs_compat"],
  d1_databases: [
    {
      binding: "DB",
      database_name: "novel-translator",
      // TODO: paste the database_id printed by
      //   npx wrangler d1 create novel-translator
      database_id: "REPLACE_ME",
      migrations_dir: "drizzle",
    },
  ],
  triggers: {
    // Every minute: pick up pending chunks and translate a batch.
    crons: ["* * * * *"],
  },
  vars: {},
}) as Config;
